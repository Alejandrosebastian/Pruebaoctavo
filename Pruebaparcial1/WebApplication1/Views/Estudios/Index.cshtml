@model IEnumerable<WebApplication1.Models.Estudios>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Detalle)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Detalle)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EstudiosId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EstudiosId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EstudiosId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
