@model IEnumerable<WebApplication1.Models.Desempleado>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.tiempo)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.fecha_ini)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.fecha_fin)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.tiempo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.fecha_ini)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.fecha_fin)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DesempleadoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DesempleadoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DesempleadoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
